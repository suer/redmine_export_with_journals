= export_with_journals
is a redmine plugin to export issues CSV with history.

= Installation
1. $ cd REDMINE_ROOT
2. $ ruby script/plugin install git://github.com/suer/redmine_export_with_journals.git
